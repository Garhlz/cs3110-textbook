# 并发

到目前为止，我们考察的都是*顺序*程序：每次只执行一个步骤，而且接下来执行哪一步是确定的。顺序程序不善于同时处理多个输入源，也只能利用单个处理器。许多现代应用则是*并发*程序。

本章将介绍 Promise，一种组织并发计算的方法，近年来在命令式 Web 编程中十分流行。我们会使用实现了 Promise 的 OCaml 库 Lwt。Lwt 的部分设计建立在*单子*（monad）这一概念之上；单子可以组织带副作用的计算，因此本章最后也会考察单子。
