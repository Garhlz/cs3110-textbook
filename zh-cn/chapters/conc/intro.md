# 并发

到目前为止，我们只考虑了“顺序”程序。
顺序程序一次只执行一步，对下一步要执行哪一步没有选择余地。
顺序程序的局限在于，它们不擅长同时处理多个输入源，而且只能在单个处理器上执行。
许多现代应用程序都是“并发”的。

在本章中，我们将了解 *Promise*，这是一种组织并发计算的方法，近来在命令式 Web 编程中变得流行。
我们将看到一个名为 Lwt 的 OCaml 库，它实现了 Promise。
该库部分基于 *monad* 的概念；monad 是一种组织带有副作用的计算的方式，因此我们将以考察 monad 结束本章。
